apply from: 'versions.gradle'

group = 'tinf13b4'
version = '0.0.1-SNAPSHOT'

apply plugin: 'eclipse'
apply plugin: 'eclipse-wtp'

apply plugin: 'java'
apply plugin: 'war'

repositories {
	mavenCentral()
}

dependencies {
	compile (
		//insert compile-deps
		[group: 'com.mysql', name: 'management', version: '1.1.6'],
		[group: 'com.sun.jdmk', name: 'jmxtools', version: '1.2.1']
	)

	testCompile(
		//insert test-compile-deps
	)
}
